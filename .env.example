# Copy this file to .env and fill in values. Never commit .env.
# Currently no secrets required — all data is fetched from public APIs.
# Add API keys here if a private data source is added in future.

# CBOE_API_KEY=
# FLATEX_CLIENT_ID=
